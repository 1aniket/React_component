
const Button = () => {
  return (
    <div className="Btn">
      <button onClick={() => window.alert("Button Clicked !")}>
        Click Me!
      </button>
    </div>
  );
};

export default Button;
